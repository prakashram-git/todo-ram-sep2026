#CLAUDE COMMANDS
/clear to start a new conversation (clear previous memory/chat history)

/compact - compress the context memory and reduct it to a summary to take forward. It frees up the context window, and reduces the cost of tokens immediately.

/usage - usage is to view the current usage in context, 5 hour limit, and weekly limit - how much is left and us up.